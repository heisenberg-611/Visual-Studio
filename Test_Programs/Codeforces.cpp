//158A
/*#include<iostream>
#include<iomanip>
using namespace std;
int main(){
    
    int n, k, s, next = 0;

    cin>>n>>k;

    vector<int> scores;

    for(int i = 0; i < n; i++)
    {
        cin >> s;
        scores.push_back(s);
    }
    for(int i = 0; i<n; i++)
    {
        if( scores[i]>=scores[k-1] && scores[i]>0)
        {
            next++;
        }
    }
    cout<<next<<endl;
    return 0;
}
#include<iostream>
using namespace std;

int main(){
    int n, k, i, a[10000], counter = 0;
    cin>>n>>k;
    for (i = 0; i<n; i++)
    {
        cin>>a[i];
    }
    for(i = 0; i<n; i++)
    {
        if(a[i] >= a[k-1] && a[i] > 0)
        {
            counter++;
        }
    }
    cout<<counter<<endl;
    return 0;
}*/
//Bit++ 282A
#include<iostream>
using namespace std;

int main(){
    
}